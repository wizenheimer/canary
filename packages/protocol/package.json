{
  "name": "@canary/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Canary daemon IPC protocol — Zod schemas and inferred types (single source of truth).",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "compile": "tsc --noEmit",
    "lint": "echo \"(no lint configured for @canary/protocol)\"",
    "test": "echo \"(no tests for @canary/protocol)\""
  },
  "dependencies": {
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@canary/typescript-config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
