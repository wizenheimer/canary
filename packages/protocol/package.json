{
  "name": "@usecanary/protocol",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Canary daemon IPC protocol — Zod schemas and inferred types (single source of truth).",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "compile": "tsc --noEmit",
    "test": "echo \"(no tests for @usecanary/protocol)\""
  },
  "dependencies": {
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@usecanary/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
