{
  "name": "@usecanary/cli-kit",
  "version": "0.4.3",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Shared Canary CLI helpers — request ids, duration/result formatting, and the root logger factory (single source of truth).",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "compile": "tsc --noEmit",
    "test": "echo \"(no tests for @usecanary/cli-kit)\""
  },
  "dependencies": {
    "@usecanary/logger": "workspace:*",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "@usecanary/config": "workspace:*",
    "@types/node": "^20.17.10",
    "typescript": "^5.8.0"
  }
}
